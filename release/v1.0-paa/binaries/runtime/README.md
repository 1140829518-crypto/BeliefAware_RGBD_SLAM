# Runtime-instrumented artifacts

These files are the lightweight-instrumentation binaries used by the dedicated
runtime campaign. They are intentionally separate from the formal accuracy
artifacts and have different hashes.

The binaries are Linux x86-64 build artifacts and still require the matching
native shared-library environment described in `../../ENVIRONMENT.md`.
