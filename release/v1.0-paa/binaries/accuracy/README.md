# Formal accuracy artifacts

The formal accuracy campaign used the two artifacts recorded in
`EXPECTED_SHA256.txt`. Their original binary files are no longer present in
the working repository and were not reconstructed or substituted during
release preparation.

In particular, the runtime-instrumented artifacts in the sibling `runtime/`
directory are not accuracy-campaign binaries and must not be renamed or used
to satisfy these hashes.

Status: **metadata frozen; binary payload unavailable**.
